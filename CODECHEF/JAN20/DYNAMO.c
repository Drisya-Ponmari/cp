#include <stdio.h>
int main()
{
	int t;
	scanf("%d",&t);
	while(t--)
	{
		int n;
		scanf("%d",&n);
		long long int a,s,b,c,d,e;
		scanf("%lld",&a);
		s = 50;
		printf("%lld",s);
		fflush(stdin);
		scanf("%lld",&b);
		c= b;
		printf("%lld",c);
		fflush(stdin);
		scanf("%lld",&d);
		e = d;
		printf("%lld",e);
		fflush(stdin);
		int re;
		scanf("%d",&re);
		if(re == -1)
			continue;
	
	}
	return 0;
}
